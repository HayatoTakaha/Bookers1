<!DOCTYPE HTML>
<html lang="ja">
<head>
    <meta charset="UTF-8"/>
</head>

  <body>

 <h1>Books</h1>

<table>
 <thead>
  <tr>
   <th>Title</th>
   <th>Body</th>
  </tr>
  </thead>

  <tbody>
    <tr>
    <% @book.each do |book| %>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <td><a href='/books'>Show</a></td>
    <td><a href="/books">Edit</a></td>
    <td><a date-confirm="Are you sure?" class="destroy" rel="nofollow" date-method="delete" href="/books">Destroy</a></td>
    </tr>
 <% end %>
 </tbody>
  </table>
  
<h2>New book</h2>

<p>title</p>
<%= form_with model: Book do |f| %>
  <%= f.text_field :title %>
  <p>body</p>
  <%= f.text_area :body %>

  <p><%= f.submit 'Create Book' %></p>
  
<% end %>


</body>
</html>

